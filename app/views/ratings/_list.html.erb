<table class="table table-hover">
  <thead>
  <tr>
    <th></th>
    <th><b>average</b></th>
  </tr>
  </thead>

  <tbody>
  <% besties.each do |b| %>
      <tr>
        <td><%= link_to b.name, b %></td>
        <td><%= round_average(b.average_rating) %></td>
      </tr>
  <% end %>
  </tbody>
</table>